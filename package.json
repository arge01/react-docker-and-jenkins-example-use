{
  "name": "@movies/app",
  "version": "0.0.1",
  "private": true,
  "description": "Movies App",
  "author": "Arif GEVENCİ",
  "author-link": "https://arifgevenci.com",
  "workspaces": [
    "app",
    "router"
  ],
  "scripts": {
    "app:build": "cd app && yarn build",
    "router:build": "cd router && yarn build",
    "app:dev": "cd app && yarn dev",
    "router:dev": "cd router && yarn dev",
    "dev": "yarn app:dev",
    "build": "yarn router:build && yarn app:build",
    "install:all": "yarn install && cd app && yarn install && cd ../router && yarn install"
  },
  "license": "MIT"
}